# ---- MedFlow Frontend env ----
# Copy to .env.local (local) and set the prod value in Vercel.

# Base URL of the backend API (must include /api, no trailing slash).
# Local: http://localhost:8000/api
# Prod:  https://<your-backend>.onrender.com/api
NEXT_PUBLIC_API_URL=http://localhost:8000/api
